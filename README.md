# Frontend:
📦 Requirements
Before starting, make sure you have the following installed:
Node.js (LTS version) → https://nodejs.org
Angular CLI → Install using: <npm install -g @angular/cli>
check angular cli installation: <ng version>



